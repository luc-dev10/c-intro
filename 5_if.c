#include <stdio.h>

int main(void) {
  // check equality
  if (1 == 1) {
    printf("equals");
  }

  // check inequality
    if (1 != 2) {
    printf("not equals");
  }

}

// 0 is always false it seems
