#include <stdio.h>
#include <string.h>

int main(void) {

  // nums
  // cannot be changed values of array  

  // returns 0 if equals
  printf("%d\n", strcmp("he", "hello") == 0);

  // length
  printf("%d\n", strlen("hello"));

  return 0;
}